If shared file does not work for your machine, use makefile to compile the C code into a shared objec. You do not need to make any changes in the makefile, just type "make" in the terminal and the C code will be compiled to give a shared object "LBM_Sequential.so".



